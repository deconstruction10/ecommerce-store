import { ErrorHandlingDirective } from './error-handling.directive';

describe('ErrorHandlingDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorHandlingDirective();
    expect(directive).toBeTruthy();
  });
});
