import { AnimatedNavbarDirective } from './animated-navbar.directive';

describe('AnimatedNavbarDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimatedNavbarDirective();
    expect(directive).toBeTruthy();
  });
});
